# LogSentinel Backend

This package contains the FastAPI application and supporting code for the LogSentinel
multi-tenant log monitoring backend. It is installed and run via `uv` in local development
and inside the Docker images.

